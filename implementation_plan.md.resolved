# 💌 Telegram-бот «Тайный Валентин»

Бот для отправки **анонимных валентинок** между пользователями Telegram. Получатель получает красивое сообщение, но не знает отправителя. Монетизация через **Telegram Stars**: открытие имени отправителя, AI-стихи, премиум-оформление.

---

## Бизнес-модель

| Действие | Цена | Описание |
|----------|------|----------|
| Базовая валентинка | **Бесплатно** (1/день) | Текст до 500 символов, стандартное оформление |
| Раскрыть отправителя | **50 ⭐** | Получатель платит, чтобы узнать кто написал |
| AI-стихотворение | **30 ⭐** | Бот генерирует уникальное стихотворение по описанию |
| Премиум валентинка | **50 ⭐** | Красивое HTML-оформление с эмодзи-арт |
| Пакет 5 валентинок | **100 ⭐** | 5 базовых валентинок за раз |

---

## Техстек

- **Python 3.11+** + `python-telegram-bot` v21 (async)
- **SQLite** + `aiosqlite` — хранение пользователей, валентинок, платежей
- **OpenAI API** (опционально) — генерация стихов, или шаблонные стихи как fallback

---

## Proposed Changes

### Структура проекта

```
/Users/a1111/Desktop/14 feburary/
├── bot.py              # Точка входа, запуск бота
├── config.py           # Настройки (токены, цены)
├── database.py         # SQLite — модели и запросы
├── handlers/
│   ├── __init__.py
│   ├── start.py        # /start, главное меню
│   ├── send.py         # Отправка валентинки
│   ├── inbox.py        # Входящие валентинки
│   ├── reveal.py       # Раскрытие отправителя (платно)
│   ├── poems.py        # Генерация стихов
│   └── payment.py      # Обработка Telegram Stars
├── templates.py        # Шаблоны сообщений и стихов
├── requirements.txt    # Зависимости
├── .env.example        # Пример .env файла
└── README.md           # Инструкция по запуску
```

---

### Компонент 1: Конфигурация и зависимости

#### [NEW] [config.py](file:///Users/a1111/Desktop/14 feburary/config.py)
- Загрузка `BOT_TOKEN`, `OPENAI_API_KEY` из переменных окружения
- Константы цен в Stars: `REVEAL_PRICE = 50`, `POEM_PRICE = 30`, `PREMIUM_PRICE = 50`, `BUNDLE_PRICE = 100`
- Лимит бесплатных валентинок в день: `FREE_DAILY_LIMIT = 1`

#### [NEW] [requirements.txt](file:///Users/a1111/Desktop/14 feburary/requirements.txt)
- `python-telegram-bot[all]>=21.0`
- `aiosqlite`
- `python-dotenv`
- `openai` (опционально)

#### [NEW] [.env.example](file:///Users/a1111/Desktop/14 feburary/.env.example)
- Шаблон с `BOT_TOKEN=` и `OPENAI_API_KEY=`

---

### Компонент 2: База данных

#### [NEW] [database.py](file:///Users/a1111/Desktop/14 feburary/database.py)

**Таблицы:**

```sql
-- Пользователи
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,      -- Telegram user ID
    username TEXT,
    first_name TEXT,
    registered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    free_sends_today INTEGER DEFAULT 0,
    last_send_date DATE
);

-- Валентинки
CREATE TABLE valentines (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    sender_id INTEGER REFERENCES users(user_id),
    receiver_id INTEGER REFERENCES users(user_id),
    message TEXT NOT NULL,
    is_premium BOOLEAN DEFAULT FALSE,
    is_poem BOOLEAN DEFAULT FALSE,
    is_revealed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Платежи
CREATE TABLE payments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER REFERENCES users(user_id),
    amount INTEGER,             -- в Stars
    type TEXT,                  -- reveal / poem / premium / bundle
    valentine_id INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Функции:** `init_db()`, `get_or_create_user()`, `create_valentine()`, `get_inbox()`, `reveal_sender()`, `can_send_free()`, `record_payment()`

---

### Компонент 3: Основной бот и меню

#### [NEW] [bot.py](file:///Users/a1111/Desktop/14 feburary/bot.py)
- Инициализация `Application`, подключение handlers
- Запуск polling

#### [NEW] [handlers/start.py](file:///Users/a1111/Desktop/14 feburary/handlers/start.py)
- Команда `/start` — приветствие + deep link обработка (если пришли по ссылке для получения валентинки)
- Главное меню через `InlineKeyboard`:
  - 💌 Отправить валентинку
  - 📬 Мои валентинки (входящие)
  - ✍️ Заказать стихотворение
  - 📊 Статистика

---

### Компонент 4: Отправка валентинок

#### [NEW] [handlers/send.py](file:///Users/a1111/Desktop/14 feburary/handlers/send.py)

**Сценарий отправки (ConversationHandler):**

1. Пользователь нажимает «💌 Отправить валентинку»
2. Бот спрашивает: «Перешлите сообщение от получателя или введите @username»
3. Пользователь указывает получателя
4. Бот спрашивает текст валентинки (или предлагает шаблоны)
5. Предпросмотр → Подтверждение
6. Валентинка отлетает получателю с кнопкой «Узнать кто отправил 💫 (50⭐)»

**Deep Link:** Отправитель получает ссылку `t.me/bot_name?start=valentine_ID` для шаринга, если получатель ещё не в боте.

---

### Компонент 5: Входящие и раскрытие

#### [NEW] [handlers/inbox.py](file:///Users/a1111/Desktop/14 feburary/handlers/inbox.py)
- Список входящих валентинок с пагинацией
- Каждая показывается как: текст + ❓ Тайный отправитель
- Кнопка «Узнать кто 💫»

#### [NEW] [handlers/reveal.py](file:///Users/a1111/Desktop/14 feburary/handlers/reveal.py)
- При нажатии на «Узнать» → создаётся Telegram Stars invoice на 50⭐
- После успешной оплаты → показывается имя и @username отправителя
- Обновление `is_revealed = TRUE` в БД

---

### Компонент 6: Оплата Telegram Stars

#### [NEW] [handlers/payment.py](file:///Users/a1111/Desktop/14 feburary/handlers/payment.py)
- `PreCheckoutQueryHandler` — подтверждение платежа
- `MessageHandler(filters.SUCCESSFUL_PAYMENT)` — обработка успешной оплаты
- **Payload формат:** `reveal_{valentine_id}`, `poem_{user_id}`, `premium_{valentine_id}`, `bundle_{user_id}`
- Запись в таблицу `payments`

---

### Компонент 7: Стихи и шаблоны

#### [NEW] [handlers/poems.py](file:///Users/a1111/Desktop/14 feburary/handlers/poems.py)
- Пользователь описывает получателя (имя, качества)
- Если `OPENAI_API_KEY` указан — генерация через GPT
- Если нет — выбор из готовых шаблонов с подстановкой имени

#### [NEW] [templates.py](file:///Users/a1111/Desktop/14 feburary/templates.py)
- 20+ шаблонов валентинок разных стилей (романтические, шуточные, дружеские)
- 10+ шаблонов стихов с placeholders `{name}`
- Премиум оформление: HTML-разметка с эмодзи-арт рамками

---

## Вирусный механизм 🔥

```mermaid
graph LR
    A[Пользователь A] -->|Отправляет валентинку| B[Бот]
    B -->|Доставляет анонимно| C[Пользователь B]
    C -->|Хочет узнать кто → Платит 50⭐| B
    C -->|Отправляет свою валентинку| D[Пользователь C]
    D -->|Платит → Отправляет свою| E[...]
```

Каждый получатель потенциально становится отправителем → **экспоненциальный рост**.

---

## Verification Plan

### Автоматическое тестирование
- Запуск бота: `python bot.py` — проверка что бот стартует без ошибок
- Проверка создания БД: при первом запуске создаются все таблицы

### Ручное тестирование (пошагово)

> [!IMPORTANT]
> Для тестирования нужен **реальный Telegram Bot Token** от @BotFather

1. **Тест /start** — отправить `/start` боту, убедиться что показывается меню с кнопками
2. **Тест отправки** — отправить валентинку самому себе по @username, убедиться что приходит анонимное сообщение
3. **Тест inbox** — проверить что валентинка появилась во входящих
4. **Тест reveal** — нажать «Узнать кто», проверить что появляется запрос на оплату Stars
5. **Тест лимита** — попробовать отправить 2-ю бесплатную валентинку, убедиться в блокировке
6. **Тест стихов** — заказать стихотворение, проверить генерацию

> [!TIP]
> Для тестирования платежей Stars используй тестовый режим BotFather: `/mybots` → Bot Settings → Payments → Test Mode
